#include <stdio.h>

/**
 * main - prints program name
 *@argc: arg numbers
 *@argv: array of arg string
 *
 * Return: Always 0.
 */

int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
