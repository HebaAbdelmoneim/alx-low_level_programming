#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int x, y;

x = 45;
while (x < 99)
{
	y = x + 1;
	while (y < 100)
	{
		if (sec
		putchar (x);
		putchar (' ');
		putchar (y);
		if (x == 98)
			break;
		putchar (',');
		putchar (' ');
		y++;
	}
	x++;
}
putchar ('\n');
return (0);
}
